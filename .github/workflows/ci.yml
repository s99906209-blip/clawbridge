name: CI

# GitHub Actions is disabled by default on forks. If this workflow shows
# no run history after Actions is enabled in Settings, it needs one
# push/PR event to register — this comment change (and its own CI run)
# is that first event.

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  lint-and-test:
    name: Lint & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test with coverage
        run: npm run test:coverage
        env:
          ACCESS_KEY: testkey123
          OPENCLAW_WORKSPACE: /tmp/claw_test_ws
          NODE_ENV: test
